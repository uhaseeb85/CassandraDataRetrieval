# Cassandra Configuration
cassandra.contactPoints=localhost
cassandra.port=9042
cassandra.localDatacenter=datacenter1
cassandra.keyspace=testks
cassandra.table=testtable
cassandra.username=
cassandra.password=
cassandra.query=SELECT * FROM testks.testtable
cassandra.batchSize=100
cassandra.totalRecords=1000

# Kafka Configuration
kafka.bootstrapServers=localhost:9092
kafka.topic=test-topic
kafka.clientId=test-client
kafka.acks=all
kafka.retries=3
kafka.batchSize=16384
kafka.bufferMemory=33554432

# Error Handling
error.maxRetries=3
error.retryBackoffMs=100

# State Checkpoint
state.checkpointFile=test-checkpoint.json 